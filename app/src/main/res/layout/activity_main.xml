<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center_horizontal"
    android:background="#28F0E3"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.wahyu.card.MainActivity"
    android:weightSum="1">

    <TextView
        android:id="@+id/textView"
        android:layout_height="38dp"
        android:text="IMAGE VIEW"
        android:gravity="left"
        android:layout_width="match_parent"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:textAppearance="@style/TextAppearance.AppCompat.Caption" />

    <Button
        android:layout_gravity="left"
        android:id="@+id/next_image"
        android:layout_width="wrap_content"
        android:layout_height="38dp"
        android:onClick="nextImage"
        android:text="Next"
        android:elevation="0dp"
        android:layout_alignRight="@+id/image_view"
        android:layout_alignEnd="@+id/image_view"
        android:layout_alignBottom="@+id/previous_image" />

    <ImageView
        android:layout_gravity="center_horizontal"
        android:id="@+id/image_view"
        android:layout_width="282dp"
        android:layout_height="292dp"
        android:src="@drawable/dua"
        android:layout_marginTop="20dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_gravity="right"
        android:id="@+id/previous_image"
        android:layout_width="wrap_content"
        android:layout_height="38dp"
        android:onClick="previousImage"
        android:text="Previous"
        android:layout_marginTop="17dp"
        android:layout_below="@+id/image_view"
        android:layout_alignLeft="@+id/image_view"
        android:layout_alignStart="@+id/image_view" />

    <TextView
        android:id="@+id/textView"
        android:layout_height="38dp"
        android:text="Creted by : Wahyu Nur A.H"
        android:gravity="center"
        android:layout_width="match_parent"
        android:textAppearance="@style/TextAppearance.AppCompat.Body2"
        android:layout_below="@+id/image_view"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_above="@+id/next_image" />

</RelativeLayout>